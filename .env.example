# Copia este fichero a .env (NO subir .env a git; ya esta en .gitignore)
# y rellena los valores reales de conexion a InfluxDB. config.py los
# carga automaticamente al arrancar mediante python-dotenv.
INFLUXDB_TOKEN=
INFLUXDB_ORG=
INFLUXDB_URL=
INFLUXDB_BUCKET=
